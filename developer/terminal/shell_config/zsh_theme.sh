### PowerLevel9K Theme load everywhere but in Ubuntu profile
source "${HOME}/.shell_config/powerlevel9krc.sh"

## Following shows usage of different prompt themes based on iTerm profile or just use one across the board
# if [[ $ITERM_PROFILE != "Ubuntu" ]]; then
#  source "${HOME}/.shell_config/powerlevel9krc.sh"
#else
#  source "${HOME}/.shell_config/zshrc_simple_prompt.sh"
#fi